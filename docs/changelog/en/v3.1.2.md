### Improved

-   Improved localized app names in browser store and extension list contexts, making browser translation, PDF translation, MV3, and open-source/free availability clearer.
-   Reduced the extension permission list by removing unused `cookies`, `webRequest`, `alarms`, and `activeTab` permissions, narrowing the permission prompts during install or update.

### Fixed

-   Fixed long-press word selection/translation being triggered accidentally when pressing or dragging on a page's native scrollbars, making scrollable content areas more stable to use.
