### Added

-   Added a layout option to auto-close the translation result panel after scrolling by roughly one viewport, reducing obstruction while continuing to read the page.

### Fixed

-   Fixed long-press translation over-blocking the `mouseup` event after a translation is triggered, reducing interference with the page's own click, selection, and interaction behavior.
-   Fixed alignment of switches in the layout settings section so the settings page stays stable after adding the new option.

### Maintenance

-   Updated the release process to use curated changelog files in three languages, keeping GitHub Release notes consistent with the repository release notes.
-   Limited build-time linting to JavaScript test files so non-target test resources do not affect packaging.
