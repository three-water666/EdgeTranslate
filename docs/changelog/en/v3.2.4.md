### Fixed

-   Fixed long-press translation detection replacing an existing page selection too early or overwriting a selection created during the gesture.
-   Fixed result panel dragging, resizing, and click-away behavior on pages containing iframes, and ensured interactions end cleanly with final dimensions saved when the window loses focus (#29, #30).

### Maintenance

-   Updated default branch references in CI and the release workflow to `main`.
